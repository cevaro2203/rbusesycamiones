package com.app.rbusesycamiones;

import android.widget.Button;

import com.google.android.material.textfield.TextInputEditText;

public class Nota {
    /*
    private static final String URL = "jdbc:postgresql://<IP>:<PORT>/<DATABASE>";
    private static final String USER = "<USERNAME>";
    private static final String PASSWORD = "<PASSWORD>";

    TextInputEditText textInputEditText;
    private String name;
    private String surNames;
    Button btn_register;

    //Creamos firebase autentication para poder crear una cuenta a traves de un email
    FirebaseAuth firebaseAuth;
    //Creamos una pequeña animaciíon en el momento de dar click en el boton registrarse
    ProgressDialog progressDialog;


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main_record);
        textInputEditText = findViewById(R.id.input_name);
        textInputEditText = findViewById(R.id.input_surNames);
        textInputEditText = findViewById(R.id.input_email);
        textInputEditText = findViewById(R.id.input_phone);
        textInputEditText = findViewById(R.id.input_password);


        btn_register = findViewById(R.id.btn_enviar_register);

        //Para obtener la fecha de registro
        Date date = new Date();

*/

}
